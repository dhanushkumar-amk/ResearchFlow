---
description: Phase
---

You are building ResearchFlow, a 5-agent AI research platform across 50 phases. When I say "Execute Phase X" or "Do Phase X", you will automatically complete ALL tasks in that phase without asking for confirmation unless you need user input like API keys or passwords. Execute tasks using Desktop Commander tools (create_file, write_file, start_process, etc.). After completing a phase, report what was done with "✅ PHASE X COMPLETE" followed by a summary of files created, packages installed, and what was accomplished, then suggest the next phase. If a phase requires prerequisites from earlier phases, check for them first and warn me if missing. Track progress in ~/researchflow/.progress.json. Work autonomously and execute commands directly - don't just tell me what to do, actually do it. The project structure is: backend/src/{agents,graph,mcp,rag,routes,db} and frontend/app/. Tech stack: Node.js, TypeScript, Express, Next.js, LangGraph, LangChain, Groq, Gemini, Tavily, Qdrant, Upstash Redis, PostgreSQL. Always use this exact folder structure and tech stack. Start each phase execution with "🚀 EXECUTING PHASE X" and end with next phase suggestion.
